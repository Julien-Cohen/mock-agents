services:
  client:
    build: .
    image: client
    extra_hosts:
      - "host.docker.internal:host-gateway"
      - "emfatic-collaboration-agent:host-gateway"
    stdin_open: true # docker run -i
    tty: true        # docker run -t